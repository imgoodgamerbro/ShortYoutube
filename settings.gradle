dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // jcenter() is deprecated, safe to remove
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Short Youtube"
include ':app'
